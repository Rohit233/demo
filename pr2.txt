#PR-2

# Basic Linux command

pwd - show present working directory

cd folderName - change directory to given folderName

ls - list out the content of present directory

cat fileName - list the contents of a file

cp sourceFile destinationFolderPath - copy the content in destinationFolderPath

mv sourceFile destinationFolderPath - move the content in destinationFolderPath

mkdi newFolder - create new Folder

rmdir newFolder - remove folder

touch file.txt - create new file


# USER MANAGEMENT

sudo adduser username

sudo passwd -l 'username'

sudo userdel -r 'username'

groupmod "Press Tab key twice"

sudo groupadd mygroup - (create new group)

sudo groupdel projectA

sudo groupmod --new-name projectZ projectA - (group name change)

sudo usermod -a -G GROUPNAME USERNAME  -(add user to a group)

sudo deluser USER GROUPNAME -(Removing a user from Usergroup)


___________________________________________________________

1.->  touch help.txt
      mkdir csed
      mv help.txt csed
      history

2.->  pwd
      ls -l
      history

3.->  mkdir cclab
      cd cclab
      touch cc.txt
      echo "Welcome to Computer Center, MIT, Aurangabad" >> cc.txt
      cat cc.txt
      history

4.->  touch cse.txt
      echo "Welcome to Department of Computer Science and
Engineering, Auranagabd" >> cse.txt 
      mkdir linux
      mkdir linux/inlinux
      cp cse.txt linux/inlinux/
      ls linux/inlinux/
      history

5.->  mkdir files
      mkdir files/students
      touch files/students/roll1.txt files/students/roll2.txt files/students/roll3.txt files/students/roll4.txt files/students/roll5.txt
      ls files/students/
      rm files/students/roll2.txt files/students/roll4.txt
      ls files/students/
      rm -r files/students/
      history

6.->  sudo adduser user1
      cat /etc/passwd
      groups user1
      sudo adduser user2
      sudo adduser user3
      sudo groupadd Dev
      sudo usermod -a -G Dev user1
      sudo usermod -a -G Dev user3
      sudo groupadd testing
      sudo usermod -a -G testing user1
      sudo usermod -a -G testing user2
      cat /etc/group
      cat /etc/passwd
      sudo userdel -r 'user1'
      sudo userdel -r 'user2'
      sudo userdel -r 'user3'
      history
